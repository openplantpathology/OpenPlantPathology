# Global settings applied to the whole site.
[build]
  command = "hugo"
  publish = "public"

[Build]
  Command = "hugo"
  Publish = "public"
  Functions = ""


[context.production.environment]
  HUGO_VERSION = "0.35"
